# 多租戶後端架構設計（職游 + 浮島）

## 核心需求

### 關鍵原則
1. **同一份 Code**：職游和浮島使用相同的程式碼庫
2. **獨立部署**：可部署到不同資料庫（職游 DB / 浮島 DB）
3. **彈性配置**：透過環境變數切換租戶模式
4. **權限隔離**：職游看全部，浮島只看自己的資料

### 業務定位
- **職游**：擁有所有資料權限（包含浮島資料）
- **浮島**：只能存取自己產生的資料（類似 Power BI 概念）
- **流程與體驗**：完全一致，僅需換皮（前端）

---

## 核心角色定義

### 雙邊業務角色對照

| 角色 | 職游 (Career Journey) | 浮島 (Floating Island) |
|------|----------------------|----------------------|
| **主要使用者** | 諮詢師 (Counselor) | 老師 (Teacher) |
| **服務對象** | 個案 (Client) | 學生 (Student) |
| **業務場景** | 職涯諮詢、心理諮商 | 升學輔導、教育諮詢 |

### 統一術語（程式碼層級）

為了維護同一份 code，**後端統一使用 `counselor` 和 `client`**

- 資料庫 table 名稱：`counselors`, `clients`
- API 路由：`/api/v1/counselors`, `/api/v1/clients`
- 前端顯示文案：根據連接的 API domain 自動調整
  - 職游 App → `api.career-journey.com` → 顯示「諮詢師」、「個案」
  - 浮島 App → `api.floating-island.com` → 顯示「老師」、「學生」

---

## 部署架構總覽表

### 完整架構對照表

| 層級 | 職游 | 浮島 | 是否共用 | 說明 |
|------|------|------|----------|------|
| **Source Code (Git Repo)** | `career_ios_backend` | `career_ios_backend` | 🟢 共用 | 同一份程式碼 |
| **CI/CD (GitHub Actions)** | 同一個 workflow | 同一個 workflow | 🟢 共用 | 同時部署兩邊 |
| **Docker Image** | 同一個 Dockerfile | 同一個 Dockerfile | 🟢 共用 | 同一個映像檔 |
| **Cloud Run 實例 (機器)** | `career-journey-api` | `floating-island-api` | 🟣 分開 | 獨立部署，環境變數不同 |
| **API Domain** | `api.career-journey.com` | `api.floating-island.com` | 🟣 分開 | 不同網域，安全隔離 |
| **資料庫實例** | 職游 Supabase | 浮島 Supabase | 🟣 分開 | 完全獨立資料庫 |
| **資料庫 Schema** | 同一套 SQL schema | 同一套 SQL schema | 🟢 共用 | 結構相同 |
| **環境變數** | `TENANT_ID=career_journey` | `TENANT_ID=floating_island` | 🟣 分開 | 不同配置 |
| **Supabase Storage** | 職游 bucket | 浮島 bucket | 🟣 分開 | 檔案隔離 |
| **Monitoring Logs** | 職游 logs | 浮島 logs | 🟣 分開 | 獨立追蹤 |
| **iOS App** | 職游 App | 浮島 App | 🟣 分開 | 不同 Bundle ID，不同文案 |

---

## 完整架構圖（含 Source Code, CI/CD, 機器）

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    Source Code (GitHub Repository)                       │
│                      career_ios_backend/                                 │
│                                                                          │
│  ├── app/                    (FastAPI 程式碼)                             │
│  ├── Dockerfile              (容器定義)                                   │
│  ├── requirements.txt        (依賴套件)                                   │
│  ├── .env.career_journey     (職游環境變數範本)                           │
│  ├── .env.floating_island    (浮島環境變數範本)                           │
│  └── .github/workflows/      (CI/CD 設定)                                │
│      └── deploy.yml          (部署兩邊的 workflow)                        │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Git Push (觸發部署)
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        GitHub Actions (CI/CD)                            │
│                                                                          │
│  Jobs:                                                                   │
│    1. Build Docker Image (同一個)                                        │
│    2. Deploy to Career Journey Cloud Run                                │
│    3. Deploy to Floating Island Cloud Run                               │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│     職游部署實例 (機器)           │  │     浮島部署實例 (機器)           │
├─────────────────────────────────┤  ├─────────────────────────────────┤
│                                 │  │                                 │
│ ☁️  GCP Cloud Run               │  │ ☁️  GCP Cloud Run               │
│    Service: career-journey-api  │  │    Service: floating-island-api │
│    Region: asia-east1           │  │    Region: asia-east1           │
│    Container: 同一個 Docker Image│  │    Container: 同一個 Docker Image│
│                                 │  │                                 │
│    環境變數:                     │  │    環境變數:                     │
│      TENANT_ID=career_journey   │  │      TENANT_ID=floating_island  │
│      DATABASE_URL=職游 DB URL   │  │      DATABASE_URL=浮島 DB URL   │
│                                 │  │                                 │
│    Domain 對應:                  │  │    Domain 對應:                  │
│      api.career-journey.com     │  │      api.floating-island.com    │
│           │                     │  │           │                     │
│           ▼                     │  │           ▼                     │
│ 🗄️  Supabase Database           │  │ 🗄️  Supabase Database           │
│    Project: career-journey-db   │  │    Project: floating-island-db  │
│    Region: ap-northeast-1       │  │    Region: ap-northeast-1       │
│                                 │  │                                 │
│    Tables (同一套 Schema):      │  │    Tables (同一套 Schema):      │
│      - counselors (諮詢師)      │  │      - counselors (老師)        │
│      - clients (個案，所有)     │  │      - clients (學生，浮島的)   │
│      - counseling_sessions      │  │      - counseling_sessions      │
│      - case_reports             │  │      - case_reports             │
│                                 │  │                                 │
│    權限 (RLS):                   │  │    權限 (RLS):                   │
│      - 可看到所有資料            │  │      - 只能看到浮島建立的資料    │
│                                 │  │                                 │
│           │                     │  │           │                     │
│           ▼                     │  │           ▼                     │
│ 📦 Supabase Storage             │  │ 📦 Supabase Storage             │
│    Bucket: career-journey-audio │  │    Bucket: floating-island-audio│
│                                 │  │                                 │
└─────────────────────────────────┘  └─────────────────────────────────┘
           ▲                                      ▲
           │                                      │
           │                                      │
┌──────────┴──────────┐              ┌───────────┴─────────┐
│ 📱 職游 iOS App      │              │ 📱 浮島 iOS App      │
│  Bundle: com.career │              │  Bundle: com.float  │
│  文案: 諮詢師、個案  │              │  文案: 老師、學生    │
└─────────────────────┘              └─────────────────────┘
```

---

## Source Code → CI/CD → 機器 流程說明

### 1. Source Code (Git Repository)

**位置**: GitHub `career_ios_backend`

**內容**:
- FastAPI 應用程式
- 統一的資料庫 Schema (SQL)
- Dockerfile (容器定義)
- CI/CD workflow (`.github/workflows/deploy.yml`)

**特點**:
- ✅ 只有一份程式碼
- ✅ 同時服務職游和浮島

---

### 2. CI/CD Pipeline (GitHub Actions)

**觸發時機**: `git push` 到 `main` 或 `staging` branch

**執行流程**:

```
Step 1: Build Docker Image
  └─ 使用 Dockerfile 建立容器映像檔
  └─ 同一個 Image 用於兩邊部署

Step 2: Deploy to Career Journey
  └─ gcloud run deploy career-journey-api
  └─ 設定環境變數: TENANT_ID=career_journey
  └─ 設定環境變數: DATABASE_URL=職游 Supabase

Step 3: Deploy to Floating Island
  └─ gcloud run deploy floating-island-api
  └─ 設定環境變數: TENANT_ID=floating_island
  └─ 設定環境變數: DATABASE_URL=浮島 Supabase
```

**結果**:
- ✅ 一次 push → 同時部署兩個 Cloud Run 實例
- ✅ 完全自動化，不需手動操作

---

### 3. 機器 (Cloud Run Instances)

**職游機器**:
- Service Name: `career-journey-api`
- Container: 從 GitHub Actions 部署的 Docker Image
- 環境變數: `TENANT_ID=career_journey`
- Domain: `api.career-journey.com`

**浮島機器**:
- Service Name: `floating-island-api`
- Container: 從 GitHub Actions 部署的 Docker Image (同一個)
- 環境變數: `TENANT_ID=floating_island`
- Domain: `api.floating-island.com`

**關鍵差異**:
- 不同的 **環境變數** → 連到不同資料庫
- 不同的 **Domain** → iOS App 連不同 URL

---

## 資料庫架構

### 統一 Schema (兩邊結構相同)

**設計原則**:
1. **tenant_id 全表標記**：即使資料庫分開，仍保留 tenant_id 用於未來彈性、資料溯源、防呆
2. **彈性 JSONB 存儲**：報告內容、背景資料等使用 JSONB，支援職游/浮島不同格式
3. **版本控制**：reports 表支援同一 session 生成多個版本
4. **審核流程**：reports 包含草稿、審核、通過狀態

**主要 Tables**:

```
counselors (諮詢師 / 老師)
├─ id (UUID)
├─ tenant_id (標記租戶)
├─ email (unique per tenant)
├─ password_hash
├─ name
├─ role (counselor | supervisor | admin)
├─ metadata (JSONB 彈性欄位)
├─ is_active
└─ created_at, updated_at

clients (個案 / 學生)
├─ id (UUID)
├─ tenant_id
├─ counselor_id (外鍵 → counselors)
├─ name_alias (化名)
├─ background_info (JSONB) -- 年齡、性別、職業等
├─ tags (TEXT[]) -- 標籤 ["職涯迷茫", "轉職"]
├─ status (active | archived)
├─ created_by_app (標記來源)
└─ created_at, updated_at

sessions (會談 / 輔導紀錄)
├─ id (UUID)
├─ tenant_id
├─ client_id (外鍵 → clients)
├─ counselor_id (外鍵 → counselors)
├─ session_date
├─ session_number (第幾次)
├─ room (會談地點，可選)
├─ input_type (audio | transcript)
├─ audio_path (Supabase Storage 路徑)
├─ transcript_raw (原始逐字稿)
├─ transcript_sanitized (脫敏後)
├─ processing_status (pending | processing | completed | failed)
├─ metadata (JSONB)
└─ created_at, updated_at

jobs (異步任務)
├─ id (UUID)
├─ tenant_id
├─ session_id (外鍵 → sessions)
├─ job_type (stt | sanitize | report_generation)
├─ status (queued | processing | completed | failed)
├─ progress (0-100)
├─ error_message
├─ started_at, completed_at
└─ created_at

reports (個案報告)
├─ id (UUID)
├─ tenant_id
├─ session_id (外鍵 → sessions)
├─ client_id (外鍵 → clients) -- 冗餘但方便查詢
├─ counselor_id (外鍵 → counselors) -- 冗餘但方便查詢
├─ report_version (1, 2, 3...) -- 版本控制
├─ report_type (legacy | enhanced | 自定義)
├─ content (JSONB) -- 報告主體（彈性結構）
├─ citations (JSONB) -- RAG 檢索的理論引用
├─ quality_metrics (JSONB) -- 質量評分
├─ agent_id (使用的 Agent ID)
├─ status (draft | under_review | approved | rejected)
├─ reviewed_by (督導 ID)
├─ reviewed_at
├─ review_comment
└─ created_at, updated_at

reminders (提醒)
├─ id (UUID)
├─ tenant_id
├─ client_id (外鍵 → clients)
├─ counselor_id (外鍵 → counselors)
├─ due_date
├─ content
├─ status (pending | completed | cancelled)
├─ completed_at
└─ created_at
```

**報告內容 JSONB 格式範例**:

職游 Enhanced 格式：
```json
{
  "format": "enhanced",
  "version": "2.0",
  "sections": {
    "client_info": {...},
    "main_issue": "...",
    "problem_development": "...",
    "multilevel_analysis": "...",
    "professional_judgment": "...",
    "goals_strategies": "...",
    "counselor_reflection": "..."
  },
  "theories_applied": ["Super生涯發展理論", "Holland類型論"],
  "metadata": {"word_count": 2500}
}
```

浮島教育諮詢格式：
```json
{
  "format": "education_counseling",
  "version": "1.0",
  "sections": {
    "student_profile": {...},
    "academic_performance": "...",
    "university_planning": [...],
    "next_steps": "..."
  },
  "metadata": {"target_universities": ["台大", "清大"]}
}
```

### Row-Level Security (RLS) 權限控制

**重要說明**：
- 由於職游和浮島使用**完全分開的資料庫**（不同 Supabase Project）
- RLS 不是必須的（資料庫層級已隔離）
- 但仍建議加上 RLS 作為**額外安全層**，防止應用層錯誤

**RLS Policy 範例**：

```sql
-- 諮商師只能看自己的個案
CREATE POLICY counselor_access ON clients
FOR SELECT
USING (counselor_id = auth.uid());

-- 諮商師只能看自己相關的會談
CREATE POLICY counselor_sessions ON sessions
FOR SELECT
USING (counselor_id = auth.uid());

-- 督導可以看所有報告
CREATE POLICY supervisor_access ON reports
FOR ALL
USING (
  (SELECT role FROM counselors WHERE id = auth.uid()) = 'supervisor'
);

-- tenant_id 驗證（防呆機制）
CREATE POLICY tenant_isolation ON clients
FOR ALL
USING (tenant_id = current_setting('app.tenant_id', true)::text);
```

**為什麼即使資料庫分開，還要 tenant_id？**
1. **未來彈性**：可能合併資料庫、做跨租戶分析
2. **資料溯源**：備份時知道來源
3. **開發/測試環境**：可以用單一 DB 測試多租戶
4. **防呆機制**：避免部署錯誤導致資料混淆

---

## API 設計

### 統一路由結構

**核心 API 端點**：

| 端點 | 職游顯示文案 | 浮島顯示文案 | 說明 |
|------|------------|------------|------|
| `GET /api/v1/clients` | 個案列表 | 學生列表 | 列出諮商師的所有個案 |
| `POST /api/v1/clients` | 建立個案 | 建立學生 | 自動標記 `tenant_id` 和 `created_by_app` |
| `GET /api/v1/clients/{id}` | 個案詳情 | 學生詳情 | 取得個案完整資料 |
| `GET /api/v1/clients/{client_id}/sessions` | 個案會談列表 | 學生輔導列表 | 列出所有會談記錄 |
| `POST /api/v1/clients/{client_id}/sessions` | 建立會談 | 建立輔導 | 建立新會談 Session |
| `POST /api/v1/sessions/{id}/upload-audio` | 上傳音訊 | 上傳音訊 | 音訊模式（觸發 STT） |
| `POST /api/v1/sessions/{id}/upload-transcript` | 上傳逐字稿 | 上傳逐字稿 | 逐字稿模式（跳過 STT） |
| `POST /api/v1/sessions/{session_id}/reports` | 生成報告 | 生成報告 | 調用 RAG Agent 生成報告 |
| `GET /api/v1/sessions/{session_id}/reports` | 報告版本列表 | 報告版本列表 | 列出所有報告版本 |
| `GET /api/v1/reports/{id}` | 取得報告 | 取得報告 | 取得特定報告內容 |
| `POST /api/v1/reports/{id}/review` | 督導審核 | 督導審核 | 督導審核報告 |

**簡化的單一步驟流程**：

```
1. 建立個案（首次）
   POST /api/v1/clients
   Body: {
     "name_alias": "小明",
     "background_info": {
       "age": "28歲",
       "gender": "男性",
       "occupation": "軟體工程師"
     },
     "tags": ["職涯迷茫", "轉職"]
   }
   Response: { "client_id": "uuid-xxx" }

2. 生成報告（單一請求完成所有事情）
   POST /api/v1/reports/generate
   Body: {
     "client_id": "uuid-xxx",
     "transcript": "案主：我最近工作很不順...",
     "session_date": "2024-10-25",
     "report_type": "enhanced",
     "rag_system": "openai"
   }

   後端自動：
   ├─ 建立 session 記錄（存逐字稿）
   ├─ 調用 RAG Agent 檢索理論
   ├─ GPT-4 生成報告
   ├─ 存報告到 DB（狀態：draft）
   └─ 返回完整結果

   Response: {
     "session_id": "uuid-aaa",
     "report_id": "uuid-bbb",
     "report": {
       "client_info": {...},
       "conceptualization": "...",
       "theories": [...],
       "dialogue_excerpts": [...]
     },
     "quality_summary": {...}
   }

3. 查看歷史記錄
   GET /api/v1/clients/{client_id}/sessions
   → 返回所有會談列表

4. 督導審核（可選）
   POST /api/v1/reports/{report_id}/review
   Body: {
     "status": "approved",
     "review_comment": "分析深入，建議補充..."
   }
```

**為什麼是單一步驟？**
- ✅ iOS 只需發一次請求（減少複雜度）
- ✅ 後端處理 session + report 建立（交易一致性）
- ✅ 降低網路往返，減少失敗率
- ✅ 前端邏輯簡化，易於維護

### 前端文案處理

iOS App 根據連接的 API domain 判斷租戶，顯示不同文案：

**職游 App**:
- API Base URL: `https://api.career-journey.com`
- 顯示: 諮詢師、個案、諮詢

**浮島 App**:
- API Base URL: `https://api.floating-island.com`
- 顯示: 老師、學生、輔導

---

## 共用 vs 分開的決策邏輯

### 🟢 共用的（提高效率）

| 項目 | 理由 |
|------|------|
| **Git Repository** | 同一份程式碼，避免重複開發 |
| **CI/CD Pipeline** | 一次部署兩邊，自動化 |
| **Docker Image** | 減少構建時間，確保一致性 |
| **資料庫 Schema** | 統一結構，降低維護成本 |
| **API 路由結構** | 統一介面，前端對接簡單 |

### 🟣 分開的（安全與隔離）

| 項目 | 理由 |
|------|------|
| **Cloud Run 實例 (機器)** | 資源隔離，獨立擴展，獨立計費 |
| **API Domain** | 安全隔離，防止跨租戶請求 |
| **資料庫實例** | 資料完全隔離，符合隱私要求 |
| **環境變數** | 不同配置，連到不同資源 |
| **Storage Bucket** | 檔案隔離，避免混淆 |
| **Monitoring Logs** | 獨立追蹤，方便 debug |
| **iOS App** | 不同客戶，不同品牌，不同文案 |

### 🟡 選擇性共用

| 項目 | 共用 | 分開 | 建議 |
|------|------|------|------|
| **OpenAI API Key** | 省成本 | 成本透明 | 初期共用，未來分開 |
| **錯誤追蹤 (Sentry)** | 統一 dashboard | 獨立專案 | 共用（用 tag 區分） |

---

## 部署流程說明

### 環境變數配置

**職游 Cloud Run**:
- `TENANT_ID=career_journey`
- `DATABASE_URL=postgresql://...@career-journey.supabase.co/...`
- `SUPABASE_URL=https://xxx.supabase.co`
- `STORAGE_BUCKET=career-journey-audio`

**浮島 Cloud Run**:
- `TENANT_ID=floating_island`
- `DATABASE_URL=postgresql://...@floating-island.supabase.co/...`
- `SUPABASE_URL=https://yyy.supabase.co`
- `STORAGE_BUCKET=floating-island-audio`

### 部署步驟

1. 開發者 `git push` 到 GitHub
2. GitHub Actions 自動觸發
3. Build 同一個 Docker Image
4. 部署到職游 Cloud Run (設定職游環境變數)
5. 部署到浮島 Cloud Run (設定浮島環境變數)
6. 兩個 Cloud Run 實例同時上線

---

## 成本估算（每月）

| 項目 | 職游 | 浮島 | 共用省下 | 總計 |
|------|------|------|---------|------|
| **Cloud Run (機器)** | $5-20 | $5-20 | - | $10-40 |
| **Supabase (兩個專案共用帳號)** | - | - | **共用 Pro 方案** | **$25** |
| **Domain** | $1 | $1 | - | $2 |
| **OpenAI API** | $10-50 | - | 共用 | $10-50 |
| **CI/CD (GitHub Actions)** | $0 | - | 共用 | $0 |
| **Monitoring (Sentry)** | $0 | - | 共用 | $0 |
| **總計** | - | - | **省 $25** | **$47-117** |

### Supabase 方案說明

**共用同一個 Supabase 帳號，開兩個專案**：
- ✅ **Free Tier** (初期)：$0/月，可開 2 個專案
  - 每個專案：500 MB 資料庫 + 1 GB 儲存空間
  - 適合 MVP 測試階段

- ✅ **Pro Plan** (正式營運)：$25/月，無限專案數量
  - 每個專案：8 GB 資料庫 + 100 GB 儲存空間
  - 職游專案 + 浮島專案共用同一個帳號

**成本優化**：
- 原本兩個獨立帳號：$25 + $25 = $50/月
- 共用帳號兩個專案：$25/月
- **省下 $25/月**

---

## 監控與維護

### 分開監控

| 項目 | 職游 | 浮島 |
|------|------|------|
| **API 健康檢查** | `api.career-journey.com/health` | `api.floating-island.com/health` |
| **Cloud Run Logs** | filter: `career-journey-api` | filter: `floating-island-api` |
| **資料庫監控** | 職游 Supabase Dashboard | 浮島 Supabase Dashboard |

### 共用監控

| 項目 | 工具 |
|------|------|
| **CI/CD 狀態** | GitHub Actions Dashboard |
| **程式碼品質** | SonarQube (同一個 repo) |
| **依賴安全掃描** | Dependabot (同一個 repo) |

---

## 總結

### 核心架構

```
Source Code (1 份)
    ↓
CI/CD (1 個 pipeline, 部署 2 次)
    ↓
   ├─→ 職游 Cloud Run (機器 1)  →  職游 Supabase
   └─→ 浮島 Cloud Run (機器 2)  →  浮島 Supabase
```

### 關鍵優勢

1. **同一份 Code** → 避免重複開發，bug 修復同步
2. **自動部署** → Git push → 自動部署兩邊
3. **安全隔離** → 不同 Domain、不同資料庫
4. **成本優化** → 共用 CI/CD、Docker Image、OpenAI Key

### iOS 開發者視角

- 職游 App → 連 `api.career-journey.com` → 看到「諮詢師」、「個案」
- 浮島 App → 連 `api.floating-island.com` → 看到「老師」、「學生」
- **不需要傳任何租戶參數**，後端自動根據 domain 識別

### 後端開發者視角

- **維護一份程式碼**
- **維護一套 Schema**
- **Git push → 自動部署兩邊**
- **環境變數控制行為**

---

## 已確認事項

1. **文案細節** ✅
   - 職游：「諮詢師」、「個案」
   - 浮島：「老師」、「學生」

2. **業務流程** ✅
   - 諮詢流程完全一樣
   - **報告格式不同**（需要不同模板）

---

## 報告格式差異處理

### 策略：完全彈性的 JSONB 架構

**報告格式特性**：
- ✅ 職游和浮島報告格式**差異很大**
- ✅ 欄位**完全浮動**，沒有固定結構
- ✅ 可能隨時新增/修改欄位

**資料庫設計（極簡彈性）**：
```
case_reports
├─ id (UUID)
├─ session_id (UUID)
├─ client_id (UUID)
├─ counselor_id (UUID)
├─ tenant_id (VARCHAR)
├─ created_by_app (VARCHAR)
├─ report_content (JSONB)  ← 完全彈性，任意結構
├─ report_pdf_url (TEXT)   ← 如果有 PDF
├─ status (VARCHAR)
├─ created_at (TIMESTAMPTZ)
└─ updated_at (TIMESTAMPTZ)
```

**report_content 範例（職游）**：
```json
{
  "title": "個案概念化報告",
  "sections": [
    {
      "name": "基本資料",
      "fields": {
        "年齡": "28歲",
        "職業": "軟體工程師"
      }
    },
    {
      "name": "主訴問題",
      "content": "職涯迷茫，不知道未來方向"
    },
    {
      "name": "個案概念化",
      "framework": "4P 分析",
      "data": {
        "predisposing": ["完美主義", "家庭期待高"],
        "precipitating": ["工作倦怠", "同儕比較"],
        "perpetuating": ["迴避決策", "過度思考"],
        "protective": ["學習能力強", "有支持系統"]
      }
    }
  ],
  "custom_fields": {
    "諮詢技巧": ["同理回應", "認知重構"],
    "理論依據": "Super 生涯發展理論"
  }
}
```

**report_content 範例（浮島）**：
```json
{
  "report_type": "升學輔導報告",
  "student_info": {
    "grade": "高三",
    "school": "XX高中",
    "rank": "10/300"
  },
  "academic_analysis": {
    "strengths": ["數學", "物理"],
    "weaknesses": ["英文作文"],
    "target_score": {
      "math": 95,
      "english": 85
    }
  },
  "university_planning": {
    "targets": ["台大電機", "清大資工", "交大資工"],
    "strategy": "申請入學為主，指考為輔",
    "timeline": [
      {"date": "2024-11", "task": "準備備審資料"},
      {"date": "2024-12", "task": "完成自傳"}
    ]
  },
  "counseling_summary": "學生學習動機強，建議加強英文作文..."
}
```

**API 實作方式**：
1. `POST /api/v1/reports`
   - 接收任意 JSON 結構
   - 直接存入 `report_content` JSONB
   - 不驗證欄位（完全彈性）

2. `GET /api/v1/reports/{id}`
   - 回傳完整 JSONB
   - 前端自行解析顯示

3. 前端根據 `tenant_id` 決定顯示方式
   - 職游 App：解析職游格式
   - 浮島 App：解析浮島格式

**優點**：
- ✅ 完全彈性，任意欄位
- ✅ 不需要 migration 就能新增欄位
- ✅ 職游和浮島可以完全不同結構
- ✅ 未來格式變更不影響資料庫

**缺點**：
- ⚠️ 無法在資料庫層級驗證欄位
- ⚠️ 查詢特定欄位較複雜（需用 JSONB 查詢語法）

**建議**：
- 在應用層（Python）定義 Pydantic Schema 驗證格式
- 資料庫層不強制，保持彈性

---

## 已確認事項（續）

3. **tenant_id 欄位** ✅ 保留
   - 保留 `tenant_id` 和 `created_by_app` 欄位
   - 用途：資料來源標記、未來擴充、除錯追蹤
   - 不用於權限控制（資料庫已分開）

---

## 已確認事項（續）

4. **報告格式** ✅ 差異很大，且都是浮動的
   - 職游報告：欄位浮動，格式不固定
   - 浮島報告：欄位浮動，格式不固定
   - **解決方案**：完全使用 JSONB，不預設任何固定欄位

---

## 未來擴充考量

### 第三個業務線（未確定）

**如果有第三個業務線，架構可直接擴充**：

1. 建立新的 Supabase 專案（同一個帳號）
2. 部署新的 Cloud Run 實例
3. 使用同一份程式碼
4. 設定新的環境變數：`TENANT_ID=new_tenant`
5. 設定新的 Domain：`api.new-tenant.com`

**成本增加**：
- Cloud Run：+$5-20/月
- Supabase：$0（共用同一個 Pro 帳號）
- Domain：+$1/月

**不需要修改程式碼**，只需要：
- 新增環境變數配置
- 新增 CI/CD deploy job
- 新增資料庫 Schema（執行同一份 SQL）

**總結：架構已具備擴充性** ✅
