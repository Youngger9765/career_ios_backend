# === PRODUCTION ENVIRONMENT ===
# This file documents the production environment variables
# Actual values are stored in GitHub Secrets (PROD_* prefix)
#
# Created: 2026-01-06
# Status: Production setup completed

# ============================================================
# ⚠️ TODO: 以下目前沿用 Staging，未來需要分開
# ============================================================

# Database (TODO: 建立獨立的 Production Database)
# 目前狀態: 沿用 Staging Supabase PostgreSQL
# 未來計畫: 建立獨立的 Production Supabase 專案
PROD_DATABASE_URL=postgresql://...      # GitHub Secret: PROD_DATABASE_URL
PROD_DATABASE_URL_DIRECT=postgresql://... # GitHub Secret: PROD_DATABASE_URL_DIRECT

# Supabase Storage (TODO: 建立獨立的 Production Supabase)
# 目前狀態: 沿用 Staging Supabase
# 未來計畫: 建立獨立的 Production Supabase 專案
PROD_SUPABASE_URL=https://xxx.supabase.co    # GitHub Secret: PROD_SUPABASE_URL
PROD_SUPABASE_ANON_KEY=xxx                   # GitHub Secret: PROD_SUPABASE_ANON_KEY
PROD_SUPABASE_SERVICE_KEY=xxx                # GitHub Secret: PROD_SUPABASE_SERVICE_KEY

# ============================================================
# ✅ 以下已獨立設定
# ============================================================

# Security (已獨立產生)
PROD_SECRET_KEY=xxx  # GitHub Secret: PROD_SECRET_KEY (獨立產生，與 staging 不同)

# App URL (部署後更新)
PROD_APP_URL=https://career-app-api-prod-xxx.run.app  # GitHub Secret: PROD_APP_URL

# ============================================================
# 共用 Secrets (Staging/Production 相同)
# ============================================================
# 以下 secrets 不需要 PROD_ 前綴，Staging/Production 共用:
# - OPENAI_API_KEY
# - ELEVEN_LABS_API_KEY
# - CODEER_API_KEY, CODEER_DEFAULT_AGENT, CODEER_AGENT_*
# - SMTP_USER, SMTP_PASSWORD, FROM_EMAIL

# ============================================================
# Cloud Run 部署設定
# ============================================================
# Service: career-app-api-prod
# Region: us-central1
# Project: groovy-iris-473015-h3
# min-instances: 1 (always warm)
# max-instances: 5
# DEBUG: false
# ENVIRONMENT: production
