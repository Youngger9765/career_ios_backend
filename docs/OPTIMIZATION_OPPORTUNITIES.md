# 性能優化機會分析

**日期**: 2025-12-31
**當前性能**: 10.24s 平均（範圍 6-14s）
**目標**: 6-8s（節省 30-40%）

---

## 🎯 優化優先級總覽

| 優化項目 | 預期節省 | 難度 | 優先級 | 風險 |
|---------|---------|------|--------|------|
| **修正 RAG 執行順序** | 0s (架構問題) | 中 | P0 | 低 |
| **Gemini Context Caching** | 2-4s (50%) | 低 | P1 | 極低 |
| **Gemini Streaming** | 0s (感知改善) | 中 | P1 | 低 |
| **並行化 RAG + DB** | 0.5-1s | 低 | P2 | 極低 |
| **RAG 結果緩存** | 0.5-1s | 低 | P2 | 低 |
| **使用 Gemini Flash Lite** | 3-5s (60%) | 低 | P3 | 中 |

---

## 🔴 P0: 修正 RAG 執行順序（架構問題）

### 當前問題

```python
# app/services/keyword_analysis_service.py:175-209

# 1. 先調用 Gemini（沒有 RAG context）
ai_response = await self.gemini_service.generate_text(
    prompt, temperature=0.3, response_format={"type": "json_object"}
)

# 2. 然後才做 RAG 檢索（但結果沒用在 Prompt 裡！）
rag_results = await self.rag_retriever.search(
    query=transcript_segment[:200],
    top_k=3,
    threshold=0.7,
    db=self.db,
    category=rag_category,
)

# 3. RAG 只被加到 metadata，沒有真正影響 AI 分析
result_data["rag_documents"] = rag_documents
```

### 問題分析

1. **RAG 在 Gemini 之後執行** ← 順序錯誤
2. **RAG context 沒有被加入 Prompt** ← 功能失效
3. **RAG 只是裝飾性地出現在結果中** ← 浪費 1.74s

### 正確做法

```python
# 修正後的順序
async def analyze_partial(...):
    # 1. 先做 RAG 檢索
    rag_results = await self.rag_retriever.search(...)
    rag_context = format_rag_context(rag_results)  # 格式化成 Prompt 用的文字

    # 2. 把 RAG context 加入 Prompt
    prompt = prompt_template.format(
        context=context_str,
        rag_knowledge=rag_context,  # ← 加入 RAG 知識！
        full_transcript=full_transcript,
        transcript_segment=transcript_segment
    )

    # 3. 才調用 Gemini（帶著 RAG context）
    ai_response = await self.gemini_service.generate_text(prompt, ...)
```

### 影響

- **性能**: 不變（總時間相同，只是順序調整）
- **品質**: 大幅提升（AI 真正能用到 RAG 知識）
- **風險**: 低（只是調整順序）

### 實作成本

- 2-3 小時
- 修改 1 個檔案
- 需要更新 Prompt template

---

## ❌ P1: Gemini Context Caching（已測試，不實作）

### 測試結果 (2025-12-31)

**實測數據**:
- 基準線（無緩存）: 2.98s 平均
- 使用緩存（後續請求）: 2.13s 平均
- **節省比例**: 28.4%（不是宣稱的 50%）
- **加速倍數**: 1.40x

**測試配置**:
- Model: gemini-2.5-flash
- Cached tokens: 1869 tokens
- Prompt tokens: 1942 tokens total
- 測試腳本: `scripts/test_vertex_ai_caching.py`

**決策**: **不實作**

**理由**:
1. **效果不如預期**: 實測只省 28.4%，遠低於宣稱的 50%
2. **API 即將棄用**: 2026-06-24 將被移除
3. **複雜度增加**: 需管理緩存生命週期、Session 綁定
4. **第一次更慢**: 建立緩存耗時 4.14s vs 基準 2.98s
5. **測試環境與實際可能有差異**: 但差距不足以支持實作

### 原理（僅供參考）

Vertex AI 支援 **Context Caching**：
- 把「不變的 context」緩存起來
- 後續請求只傳「新的對話內容」
- 理論上可省 prompt tokens 處理時間，但**實測效果有限**

### 適用場景

```
不變的部分（可緩存）:
├─ 8 大教養流派理論（固定）
├─ 200+ 建議句庫（固定）
├─ Case 資訊（同一個 case 內不變）
└─ 完整對話逐字稿（累積型，可用前一次的緩存）

變化的部分（每次傳）:
└─ 最近 60 秒對話片段（唯一變化）
```

### 實作方式

```python
from google.generativeai import caching

# 1. 建立緩存（第一次）
cache = caching.CachedContent.create(
    model='gemini-2.0-flash-exp',
    system_instruction="""你是親子教養專家，精通 8 大流派...""",
    contents=[
        {
            "role": "user",
            "parts": [{"text": f"完整對話逐字稿: {full_transcript}"}]
        }
    ],
    ttl=datetime.timedelta(minutes=60),  # 緩存 60 分鐘
)

# 2. 使用緩存（後續請求）
model = genai.GenerativeModel.from_cached_content(cache)
response = model.generate_content(f"最近對話: {recent_segment}")
```

### 預期效果

**第一次請求**:
- 時間: 8.5s（不變）
- 成本: $0.26

**後續請求**（使用緩存）:
- 時間: **4-5s**（省 50%）← 因為不用處理前面的 context
- 成本: **$0.13**（省 50%）← Cached tokens 便宜 90%

**對於 1 小時會談**:
```
當前: 8.5s × 60 次 = 510s 總處理時間
優化後:
  第 1 次: 8.5s
  第 2-60 次: 4.5s × 59 = 265s
  總計: 273.5s（省 46%）
```

### 實作成本

- 4-6 小時
- 需要改 Gemini service 層
- 需要管理緩存生命週期

### 風險

- **極低**: Google 官方支援，穩定可靠
- 需要處理緩存過期（60 分鐘後自動失效）

---

## 🟡 P1: Gemini Streaming（改善感知速度）

### 原理

使用 Gemini **Streaming API**：
- 不等完整回應，逐 Token 返回
- 前端可以更早開始顯示結果
- **實際時間不變，但用戶感覺更快**

### 對比

**當前（Non-streaming）**:
```
0s ───────────────────→ 8.5s ──→ 9s
   [等待...]              [完整結果]
                          ↑ 用戶才看到第一個字
```

**優化後（Streaming）**:
```
0s ──→ 1s ──→ 2s ──→ ... ──→ 8.5s ──→ 9s
   [等]  [文] [字] [逐] [漸] [出現]
          ↑ 用戶 1 秒後就看到第一個字！
```

### 實作方式

```python
# Backend
async def analyze_partial_streaming(...):
    # 使用 Gemini streaming
    response = await gemini.generate_content_stream(prompt)

    async for chunk in response:
        # 逐步發送給前端
        yield {"chunk": chunk.text, "done": False}

    yield {"chunk": "", "done": True, "result": final_result}

# Frontend
const stream = await fetch('/api/analyze-partial-stream', {
    method: 'POST',
    body: JSON.stringify({...})
});

const reader = stream.body.getReader();
while (true) {
    const {done, value} = await reader.read();
    if (done) break;

    // 即時更新 UI
    updateSuggestionText(value.chunk);
}
```

### 預期效果

- **實際時間**: 8.5s（不變）
- **感知時間**: 1-2s（用戶看到第一個字的時間）
- **體驗提升**: 巨大（從「等 8 秒」變成「1 秒開始看到內容」）

### 實作成本

- 6-8 小時
- 需要改 API 端點（支援 SSE 或 WebSocket）
- 需要改前端（處理 streaming）

### 風險

- **低**: 技術成熟，但需要處理連線中斷

---

## 🟢 P2: 並行化 RAG + DB 查詢（省 0.5-1s）

### 當前問題

```python
# 當前是串行執行
context_str = self._build_context(session, client, case)  # 需要 DB 查詢
rag_results = await self.rag_retriever.search(...)        # 需要 RAG 查詢

# 這兩個其實可以並行！
```

### 優化方式

```python
import asyncio

# 並行執行
context_task = asyncio.create_task(self._build_context_async(...))
rag_task = asyncio.create_task(self.rag_retriever.search(...))

# 等待兩個都完成
context_str, rag_results = await asyncio.gather(context_task, rag_task)
```

### 預期效果

```
當前（串行）:
DB 查詢 (0.2s) ──→ RAG 檢索 (1.74s) = 1.94s

優化後（並行）:
DB 查詢 (0.2s)  ┐
                ├─→ max(0.2s, 1.74s) = 1.74s
RAG 檢索 (1.74s)┘

節省: 0.2s
```

### 實作成本

- 2-3 小時
- 風險極低

---

## 🟢 P2: RAG 結果緩存（省 0.5-1s）

### 原理

- 相似的對話片段 → 相似的 RAG 查詢
- 可以緩存 RAG 檢索結果
- 用 Redis 或記憶體緩存

### 實作方式

```python
from functools import lru_cache
import hashlib

@lru_cache(maxsize=100)
async def search_with_cache(query_hash: str, category: str):
    # 實際 RAG 檢索
    return await rag_retriever.search(...)

# 使用時
query_hash = hashlib.md5(query[:100].encode()).hexdigest()
rag_results = await search_with_cache(query_hash, category)
```

### 預期效果

- **緩存命中**: 0ms（vs 1.74s）
- **命中率**: 10-20%（估算，需實測）
- **平均節省**: 0.2-0.3s

### 實作成本

- 3-4 小時
- 需要管理緩存失效

---

## 🟣 P3: 使用 Gemini Flash Lite（省 3-5s，有風險）

### 原理

Gemini 有多個模型可選：
- **Gemini 2.0 Flash** (當前): 高品質，速度中等
- **Gemini 1.5 Flash-8B**: 更快，品質略降
- **Gemini Nano**: 極快，品質明顯降低

### 對比測試需求

需要先測試品質差異：
```
1. 準備 50 個真實對話案例
2. 分別用 Flash vs Flash-8B 分析
3. 比較:
   - Safety level 判斷準確率
   - 建議品質
   - 速度差異
```

### 預期效果

```
Gemini 2.0 Flash: 8.5s，高品質
Gemini 1.5 Flash-8B: 3-4s，品質 -10%?

需實測確認品質損失是否可接受
```

### 風險

- **中**: 品質可能下降
- 需要 A/B 測試確認

---

## 📊 綜合優化方案

### 方案 A：保守優化（推薦）

```
1. P0: 修正 RAG 順序          → 品質大幅提升
2. P1: Gemini Context Caching → 省 50% (4s)
3. P2: 並行化                 → 省 0.2s
4. P2: RAG 緩存               → 省 0.2s

總節省: ~4.4s
優化後: 10.24s → 5.8s (43% 改善)
風險: 極低
成本: 10-15 小時
```

### 方案 B：激進優化

```
方案 A
+ P1: Streaming               → 感知速度大幅提升
+ P3: Gemini Flash-8B         → 再省 3-4s（需測試品質）

總節省: ~7-8s
優化後: 10.24s → 2-3s (70-80% 改善)
風險: 中（品質需驗證）
成本: 20-25 小時
```

---

## 🎯 建議執行順序

### 第 1 週：快速見效

1. ✅ 修正 RAG 順序（2-3h）
2. ✅ 並行化 RAG + DB（2-3h）
3. ✅ RAG 結果緩存（3-4h）

**預期改善**: 0.4-0.6s（小改善，但為 Caching 鋪路）

### 第 2 週：核心優化

4. ✅ Gemini Context Caching（4-6h）
5. ✅ 測試 + 調優（2-3h）

**預期改善**: 再省 4s（累計 4.4-4.6s）

### 第 3 週：體驗優化

6. ✅ Gemini Streaming（6-8h）
7. ✅ 前端整合 + 測試（4-6h）

**預期改善**: 感知速度大幅提升

### 可選：第 4 週

8. ⚠️ 測試 Gemini Flash-8B（6-8h）
9. ⚠️ A/B 測試品質（需真實用戶數據）

**預期改善**: 再省 3-4s（如果品質可接受）

---

## 📈 預期成果

### 短期（1-2 週）

```
當前: 10.24s 平均
優化後: 5.8s 平均
改善: 43%
成本: 10-15 小時開發
```

### 中期（3-4 週）

```
當前: 10.24s 平均
優化後: 2-3s 平均（如果 Flash-8B 品質可接受）
改善: 70-80%
成本: 20-30 小時開發
```

### 用戶體驗

```
當前: 15s 端到端
優化後（保守）: 9-10s 端到端
優化後（激進）: 5-6s 端到端

配合 Streaming:
感知延遲: 1-2s（看到第一個字）
```

---

**總結**:
- **P0+P1+P2** 是必做項（低風險，高回報）
- **P1 Streaming** 強烈推薦（體驗提升巨大）
- **P3 Flash-8B** 需謹慎評估（可能影響品質）
